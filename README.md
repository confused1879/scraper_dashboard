# LinkedIn Schools Dashboard

A Streamlit dashboard for exploring LinkedIn profiles from various schools.

## Setup
1. Install requirements: `pip install -r requirements.txt`
2. Run dashboard: `streamlit run dashboard.py` 